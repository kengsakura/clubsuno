# 🚀 Quick Start Guide

## ขั้นตอนการเริ่มต้นใช้งาน

### 1️⃣ ตั้งค่า Supabase Database

1. ไปที่ https://supabase.com และสร้างโปรเจคใหม่
2. ไปที่ **SQL Editor**
3. **รัน SQL นี้ก่อน** เพื่อแก้ปัญหา RLS:

```sql
-- ลบ policy เก่าทั้งหมด
DROP POLICY IF EXISTS "Users can view their own profile" ON public.profiles;
DROP POLICY IF EXISTS "Teachers can view all profiles" ON public.profiles;
DROP POLICY IF EXISTS "Users can view own profile" ON public.profiles;
DROP POLICY IF EXISTS "Teachers can view students" ON public.profiles;
DROP POLICY IF EXISTS "Users can update their own profile" ON public.profiles;
DROP POLICY IF EXISTS "Users can update own profile" ON public.profiles;
DROP POLICY IF EXISTS "Teachers can update student credits" ON public.profiles;
DROP POLICY IF EXISTS "Teachers can update all profiles" ON public.profiles;
DROP POLICY IF EXISTS "Teachers can update students" ON public.profiles;

-- สร้าง policy ใหม่แบบง่าย
CREATE POLICY "Allow all authenticated users"
  ON public.profiles
  FOR ALL
  TO authenticated
  USING (true)
  WITH CHECK (true);
```

4. จากนั้น **รันไฟล์ `supabase-schema.sql`** ทั้งหมด

### 2️⃣ คัดลอก API Keys

1. ไปที่ **Settings** → **API**
2. คัดลอก:
   - **Project URL**: `https://xxxxx.supabase.co`
   - **anon public key**: `eyJhbGc...`

### 3️⃣ ตั้งค่า Environment

สร้างไฟล์ `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
```

### 4️⃣ รันโปรเจค

```bash
npm install
npm run dev
```

เปิด http://localhost:3000

### 5️⃣ สร้างบัญชี Admin

1. ไปที่ `/login`
2. คลิก **สมัครสมาชิก**
3. กรอกข้อมูล (จะเป็น student ก่อน)
4. ไปที่ Supabase → **Table Editor** → **profiles**
5. เปลี่ยน `role` จาก `student` เป็น `teacher`
6. ล็อกเอาท์และล็อกอินใหม่

### 6️⃣ ตั้งค่า API Keys

1. ล็อกอินด้วยบัญชี Admin
2. คลิกปุ่ม **⚙️ ตั้งค่า API Keys**
3. กรอก:
   - **Suno API Key** จาก https://kie.ai
   - **AI API Key** จาก:
     - OpenAI: https://platform.openai.com
     - Anthropic: https://console.anthropic.com
     - Gemini: https://ai.google.dev
   - เลือก **AI Provider** และ **Model**
   - ตั้ง **เครดิตต่อเพลง** (แนะนำ 1)
4. กดบันทึก

### 7️⃣ ทดสอบระบบ

#### สร้างบัญชีนักเรียน:
1. เปิดหน้า `/login` ใน Private/Incognito window
2. สมัครบัญชีใหม่ (จะเป็น student อัตโนมัติ)

#### เติมเครดิต:
1. ในหน้า Admin → กรอกจำนวนเครดิต → กด "เติม"

#### สร้างเพลง:
1. ล็อกอินด้วยบัญชี student
2. ใส่ธีม → กด "เจนเนื้อเพลงด้วย AI"
3. ปรับแก้เนื้อเพลง
4. กด "สร้างเพลง"
5. รอ 1-3 นาที
6. เพลงจะปรากฏพร้อมเล่นได้

## 🐛 แก้ปัญหา

### ปัญหา: Error loading students: infinite recursion
**แก้ไข**: รัน SQL จากขั้นตอนที่ 1 อีกครั้ง

### ปัญหา: ยังไม่มีนักเรียนในระบบ
**ปกติ**: สมัครบัญชีนักเรียนใหม่ได้ที่ `/login`

### ปัญหา: เครดิตไม่เพียงพอ
**แก้ไข**: Admin เติมเครดิตให้ที่หน้า `/admin`

### ปัญหา: ไม่สามารถเจนเนื้อเพลงได้
**แก้ไข**: ตรวจสอบ AI API Key ในหน้า `/admin/settings`

### ปัญหา: การสร้างเพลงล้มเหลว
**แก้ไข**:
- ตรวจสอบ Suno API Key
- เช็คว่ามีเครดิตใน kie.ai
- ตรวจสอบเนื้อเพลงไม่มีคำหยาบ

## 📚 เอกสารเพิ่มเติม

ดู [README.md](README.md) สำหรับรายละเอียดเพิ่มเติม

## 🎯 Checklist

- [ ] ตั้งค่า Supabase และรัน SQL
- [ ] คัดลอก API Keys ใส่ .env.local
- [ ] npm install && npm run dev
- [ ] สร้างบัญชี Admin และเปลี่ยน role
- [ ] ตั้งค่า API Keys ในหน้า /admin/settings
- [ ] สร้างบัญชีนักเรียนทดสอบ
- [ ] เติมเครดิตให้นักเรียน
- [ ] ทดสอบสร้างเพลง

✅ เสร็จแล้ว! ระบบพร้อมใช้งาน 🎵
